
// This file is automatically generated. Do not edit it directly.
import { createClient } from '@supabase/supabase-js';
import type { Database } from './types';

// Determine if we're in production based on the URL
const isProduction = window.location.hostname !== 'localhost' && 
                    !window.location.hostname.includes('preview') && 
                    !window.location.hostname.includes('staging');

// Use the same Supabase project in production and development
const SUPABASE_URL = "https://enrctsghtmdgzkpyhsrg.supabase.co";
const SUPABASE_PUBLISHABLE_KEY = "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImVucmN0c2dodG1kZ3prcHloc3JnIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NDE4OTcwNjksImV4cCI6MjA1NzQ3MzA2OX0.b9J4D33pTodWbP3cmv7uLY5X69UPoOepTYwyjefINJo";

// Configure client options based on environment
const clientOptions = {
  auth: {
    persistSession: true,
    // In production, we want to ensure security by only allowing HTTPS
    autoRefreshToken: true,
    detectSessionInUrl: !isProduction,
  },
};

// Import the supabase client like this:
// import { supabase } from "@/integrations/supabase/client";

export const supabase = createClient<Database>(
  SUPABASE_URL, 
  SUPABASE_PUBLISHABLE_KEY,
  clientOptions
);

// Log environment info for debugging
console.log(`Supabase client initialized in ${isProduction ? 'production' : 'development'} mode`);
